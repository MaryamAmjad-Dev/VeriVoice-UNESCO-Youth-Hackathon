'use client';
import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { useState } from 'react'; import { Button } from '../ui/Button'; import { ThemeToggle } from '../shared/ThemeToggle'; import { LanguageSwitcher } from '../shared/LanguageSwitcher'; import { useLanguage } from '@/providers/LanguageProvider';
export function Header(){const router=useRouter();const[open,setOpen]=useState(false);const{t}=useLanguage();const nav=[['/#features',t('features')],['/#how-it-works',t('how')],['/#mission',t('mission')],['/#team',t('team')]];const go=()=>{setOpen(false);router.push('/workspace')};return <header className="sticky top-0 z-50 w-full border-b border-zinc-200/60 bg-white/70 backdrop-blur-xl dark:border-white/[.06] dark:bg-zinc-950/70"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-6"><Link href="/" className="group flex items-center gap-2.5"><b className="relative rounded-xl bg-gradient-to-br from-blue-600 to-indigo-500 px-3 py-2 text-white shadow-lg shadow-blue-500/25 transition-transform duration-300 group-hover:scale-105">V<span aria-hidden="true" className="absolute inset-0 -z-10 rounded-xl bg-blue-500/40 blur-md opacity-0 transition-opacity duration-300 group-hover:opacity-100"/></b><span className="text-xl font-bold tracking-tight">VeriVoice</span></Link><nav className="hidden gap-7 md:flex">{nav.map(([href,label])=><Link key={href} href={href} className="vv-navlink text-sm font-medium text-zinc-600 transition-colors hover:text-blue-600 dark:text-zinc-300 dark:hover:text-blue-400">{label}</Link>)}</nav><div className="hidden items-center gap-3 md:flex"><LanguageSwitcher/><ThemeToggle/><Button variant="outline" size="sm" onClick={go}>{t('workspace')}</Button><Button size="sm" onClick={go}>{t('start')}</Button></div><div className="flex gap-3 md:hidden"><ThemeToggle/><button onClick={()=>setOpen(!open)} aria-label="Toggle navigation" className="rounded-lg border border-zinc-200/70 px-3 py-1.5 text-sm font-medium dark:border-white/10">{open?'Close':'Menu'}</button></div></div>{open&&<nav className="vv-mobile-menu space-y-3 border-t border-zinc-200/60 bg-white/90 px-6 py-5 backdrop-blur-xl dark:border-white/[.06] dark:bg-zinc-950/90 md:hidden">{nav.map(([href,label])=><Link key={href} href={href} onClick={()=>setOpen(false)} className="block text-sm font-medium text-zinc-700 dark:text-zinc-200">{label}</Link>)}<LanguageSwitcher className="w-full [&>select]:w-full"/><Button className="w-full" onClick={go}>{t('start')}</Button></nav>}</header>}