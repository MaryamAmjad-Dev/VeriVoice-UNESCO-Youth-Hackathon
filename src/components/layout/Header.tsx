'use client';
import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { useState } from 'react'; import { Button } from '../ui/Button'; import { ThemeToggle } from '../shared/ThemeToggle'; import { useLanguage } from '@/providers/LanguageProvider';
export function Header(){const router=useRouter();const[open,setOpen]=useState(false);const{t}=useLanguage();const nav=[['/#features',t('features')],['/#how-it-works',t('how')],['/#mission',t('mission')],['/#team',t('team')]];const go=()=>{setOpen(false);router.push('/workspace')};return <header className="sticky top-0 z-50 w-full border-b border-zinc-200/50 bg-white/80 backdrop-blur-md dark:border-zinc-800/40 dark:bg-zinc-950/80"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-6"><Link href="/" className="flex items-center gap-2.5"><b className="rounded-xl bg-blue-600 px-3 py-2 text-white">V</b><span className="text-xl font-bold">VeriVoice</span></Link><nav className="hidden gap-6 md:flex">{nav.map(([href,label])=><Link key={href} href={href} className="text-sm hover:text-blue-600">{label}</Link>)}</nav><div className="hidden items-center gap-3 md:flex"><ThemeToggle/><Button variant="outline" size="sm" onClick={go}>{t('workspace')}</Button><Button size="sm" onClick={go}>{t('start')}</Button></div><div className="flex gap-3 md:hidden"><ThemeToggle/><button onClick={()=>setOpen(!open)} aria-label="Toggle navigation">{open?'Close':'Menu'}</button></div></div>{open&&<nav className="space-y-3 border-t bg-white px-6 py-5 dark:bg-zinc-950 md:hidden">{nav.map(([href,label])=><Link key={href} href={href} onClick={()=>setOpen(false)} className="block">{label}</Link>)}<Button className="w-full" onClick={go}>{t('start')}</Button></nav>}</header>}