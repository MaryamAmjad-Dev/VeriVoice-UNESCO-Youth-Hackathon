'use client';import{useRouter}from'next/navigation';import{useLanguage}from'@/providers/LanguageProvider';import{Button}from'../ui/Button';export function CTA(){const router=useRouter();const{t}=useLanguage();return <section className="bg-zinc-50 py-20 dark:bg-zinc-950"><div className="vv-cta-surface mx-auto max-w-5xl rounded-3xl bg-gradient-to-r from-blue-600 to-indigo-700 p-10 text-center text-white"><h2 className="text-3xl font-extrabold">{t('ctaTitle')}</h2><p className="mt-4">{t('heroText')}</p><div className="mt-7 flex justify-center gap-4"><Button variant="secondary" onClick={()=>router.push('/workspace')}>{t('openWorkspace')}</Button><Button variant="outline" onClick={()=>document.getElementById('how-it-works')?.scrollIntoView({behavior:'smooth'})}>{t('seeHow')}</Button></div></div></section>}