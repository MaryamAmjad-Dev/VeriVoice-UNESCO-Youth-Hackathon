'use client';
import { useRouter } from 'next/navigation'; import { Button } from '../ui/Button'; import { languages, useLanguage } from '@/providers/LanguageProvider';
export function Hero(){const router=useRouter();const{selectedLanguage,setSelectedLanguage,t}=useLanguage();const bars=[16,28,48,72,32,60,80,52,96,64,40,24,56,36,16];return <section className="relative overflow-hidden bg-white py-20 dark:bg-zinc-950 lg:py-28"><div className="vv-ambient-orb pointer-events-none absolute left-1/2 top-1/4 h-[400px] w-[400px] -translate-x-1/2 -translate-y-1/2 rounded-full bg-blue-500/10 blur-[80px]"/><div className="relative mx-auto grid max-w-7xl gap-12 px-6 lg:grid-cols-12"><div className="vv-hero-copy space-y-6 text-center lg:col-span-7 lg:text-start"><div className="inline-flex rounded-full bg-blue-50 px-3 py-1 text-xs font-semibold text-blue-700 dark:bg-blue-950 dark:text-blue-300">UNESCO Youth Hackathon 2026 Project</div><h1 className="text-4xl font-extrabold dark:text-zinc-50 sm:text-5xl">{t('heroTitle')}<br/><span className="bg-gradient-to-r from-blue-600 to-teal-500 bg-clip-text text-transparent">{t('heroAccent')}</span></h1><p className="text-lg text-zinc-600 dark:text-zinc-400">{t('heroText')}</p><div><p className="text-xs font-semibold text-zinc-600 dark:text-zinc-300">{t('language')}: <span className="text-blue-600">{selectedLanguage}</span></p><div className="mt-3 flex flex-wrap justify-center gap-2 lg:justify-start">{languages.map(l=><button key={l} type="button" aria-pressed={l===selectedLanguage} onClick={()=>setSelectedLanguage(l)} className={`rounded-md border-0 px-2.5 py-1 text-xs transition-all ${l===selectedLanguage?'bg-blue-600 text-white':'bg-zinc-100/70 hover:bg-zinc-200/70 dark:bg-zinc-900 dark:hover:bg-zinc-800'}`}>{l}</button>)}</div></div><div className="flex flex-col justify-center gap-4 sm:flex-row lg:justify-start"><Button size="lg" onClick={()=>router.push('/workspace')}>{t('start')}</Button><Button variant="outline" size="lg" className="border-0 bg-zinc-100/70 text-zinc-800 hover:bg-zinc-200 dark:border-0 dark:bg-zinc-900 dark:text-zinc-100 dark:hover:bg-zinc-800" onClick={()=>document.getElementById('how-it-works')?.scrollIntoView({behavior:'smooth'})}>{t('seeHow')}</Button></div></div><div className="flex items-center justify-center lg:col-span-5"><div className="flex aspect-square w-full max-w-[360px] flex-col justify-between rounded-3xl border p-6 dark:border-zinc-800"><div className="flex justify-between text-xs"><span>{t('language')}</span><span>UI PREVIEW</span></div><div className="flex flex-1 items-center justify-center gap-1.5">{bars.map((h,i)=><span key={i} className="w-1.5 rounded-full bg-gradient-to-t from-blue-600 to-teal-400 animate-waveform" style={{height:h,animationDelay:`${i*80}ms`}}/>)}</div><div className="rounded-xl border p-3"><b>{t('language')}</b><p key={selectedLanguage}>{t('selected')}: {selectedLanguage}</p><span className="text-blue-600">{t('ready')}</span></div></div></div></div></section>}